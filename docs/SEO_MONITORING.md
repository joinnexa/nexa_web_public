# Weekly SEO monitoring — joinnexa.ma

1. GSC branded queries: `nexa`, `joinnexa`, Arabic/French equivalents you care about.
2. Confirm Indexed: `/`, `/about-nexa/`, localized about URLs.
3. After each `vite build`, confirm `dist/about-nexa/` and `dist/fr/about-nexa/` folders exist on Vercel output.
4. Watch for duplicate-content flags vs `nexapay.ma` / `nexago.ma` — each domain should stick to distinct copy (already differentiated in explainer pages).
